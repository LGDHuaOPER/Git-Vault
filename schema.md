# SCHEMA\.md（LLM\-Wiki 全局规范与约束）

**作用**：定义 LLM 维护 Wiki 的所有规则、目录语义、页面结构、元数据、写作风格、更新策略、错误修复标准。
**地位**：最高优先级，所有 Wiki 内容生成、修改、整理、纠错必须完全遵守本文件。

---

## 1\. 全局核心原则（不可违背）

- **raw 只读原则**：`Clippings` 目录和 `raw` 目录的所有文件为原始事实源，AI 禁止修改、删除、覆盖，只允许读取。

- **wiki 结构化原则**：`wiki` 目录是整理、归纳、结构化、去重、融合后的知识，必须高度精炼、可溯源。注：当前采用仓库隔离方案，放置于另一个 `Git-Vault_LLM-Wiki` 仓库中。

- **可溯源原则**：所有 wiki 结论必须绑定 raw 数据源，禁止凭空产出内容。

- **只增不改事实原则**：新资料冲突旧资料时，不删除旧内容，而是标注更新、对比、说明差异。

- **双向链接优先**：所有相关概念、实体必须使用 \[\[页面名\]\] 内链关联。

---

## 2\. 目录语义规范（AI 必须严格区分）

对应你之前的完整目录结构，此处定义每个文件夹的**写入规则**。

### 2\.1 `Clippings/` 和 `raw/` 原始层（只读）

```plaintext
Git-Vault_WeChat/
├── Clippings/                     # 剪藏素材库：【只读】【AI 只读取需要处理的文件夹及其子孙文件夹内容，禁止写入修改】
│   ├── 微信公众号文章/              # WeChat Obsync 插件的同步文件夹：【AI 处理】【人工不需要处理】
│   └── Inbox/                     # 剪藏插件的收件箱：【AI 不需要处理】【人工处理】
│
├── Inbox/                         # 收件箱：【AI 不需要处理】【人工处理】
│
└── raw/                           # 原始素材库：【只读】【AI 只读取文件夹及其子孙文件夹内容，禁止写入修改】
    ├── assets/                    # 图片、截图、附件
	│
    └── sources/                   # 待导入新资料：【不可变】
		├── articles/              # 网页、公众号、博客正文
		├── books/                 # 书籍笔记与划线
		├── meetings/              # 会议记录/纪要、复盘报告
		├── notes/                 # 个人零散原始笔记
		│   └── social/            # 社群平台导入（weibo/ 等）
		├── papers/                # 论文、期刊、专利、标准
		├── podcasts/              # podcast 逐字稿
		├── projects/              # 专案相关素材
		├── prs/                   # 代码 PR 记录、评审意见
		├── transcripts/           # 访谈、对话记录
		└── others/                # 其他零散素材，如书签、笔记碎片、网页剪藏等
```

AI 职责：永不修改 `Clippings/` 和 `raw/`，只解析、提炼、结构化写入 wiki。

### 2\.2 `wiki/` 知识层（AI 唯一读写区）

> 注：当前采用仓库隔离方案，放置于另一个 `Git-Vault_LLM-Wiki` 仓库中

- **index\.md**：全局目录，自动维护所有词条入口。

- **sources/**：每篇 raw 对应一篇 source 摘要，记录原文核心信息、出处、时间。

- **concepts/**：理论、算法、架构、方法论、知识点。

- **entities/**：模型、项目、人物、公司、产品、工具。

- **comparisons/**：技术对比、方案对比、模型对比、优劣对比。

- **synthesis/**：跨资料整合的长文综述、专题总结。

- **queries/**：高质量问答沉淀，可复用知识问答。

- **glossary\.md**：全局术语统一解释。

---

## 3\. 所有 Wiki 页面统一模板（强制）

所有 wiki 下的 md 文件必须包含以下 frontmatter，字段不允许缺失。

```Plain Text
---
title: ""
summary: ""
tags: []
related: []
sources: []
confidence: high / medium / low
updated: YYYY-MM-DD
status: active / deprecated / draft
---
```

### 字段释义

- **title**：页面标准名称（统一、简洁、无歧义）

- **summary**：一句话摘要，用于索引预览

- **tags**：分类标签，用于聚类检索

- **related**：相关页面内链列表

- **sources**：对应 raw 原始文件路径

- **confidence**：信息可信度评级

- **updated**：最后更新日期

- **status**：页面状态，废弃内容标记 deprecated，草稿标记 draft

---

## 4\. 页面内容写作规范

### 4\.1 内容结构固定四段式

1. **定义**：精准、无歧义的核心定义

2. **核心原理/要点**：结构化分点说明

3. **优缺点、适用场景、边界**

4. **关联与溯源**：链接相关词条 \+ 引用来源

### 4\.2 风格约束

- 语言精炼、知识密度高，禁止废话、口水话

- 只保留**长期有效**的知识，临时信息不入 wiki

- 冲突信息不删除，保留新旧版本并标注差异原因

- 所有专有名词统一对齐 glossary

---

## 5\. AI 自动维护规则（最重要）

### 5\.1 新增资料入库流程（ingest）

1. 读取 `Clippings/` 和 `raw/` 新资料

2. 生成 `wiki/sources` 摘要页

3. 提炼新概念、新实体、新对比

4. 新建或更新 `concepts` / `entities` / `comparisons`

5. 更新 `index.md` 索引

6. 建立双向内链

### 5\.2 定期自检修复（lint）

AI 定期自动执行：

- 修复孤立页面（无入链页面补齐链接）

- 消除重复内容，合并同源知识

- 更新过时结论，标注版本差异

- 补全缺失 frontmatter 字段

- 统一术语、统一格式

### 5\.3 更新策略

- 事实更新：叠加更新，保留历史观点

- 结构优化：允许重排、精炼、去冗余

- 错误修正：必须标注修正原因与依据

---

## 6\. 命名规范

- 文件名：纯中文/英文，无特殊符号、无空格

- 词条名：使用行业通用标准名，不自创别名

- 统一使用 PascalCase / 标准中文命名，不混用

---

## 7\. 禁止行为（AI 红线）

- 禁止修改、删除 raw 目录任意内容

- 禁止无依据编造知识

- 禁止删除有价值历史信息

- 禁止遗漏溯源 sources 字段

- 禁止产生重复词条（需合并）

---

## 8\. 生效优先级

**SCHEMA\.md \> 目录结构 \> 页面模板 \> 历史内容**
任何旧内容不符合本 schema，以本文件为准自动修正。

